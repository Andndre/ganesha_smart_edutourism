{
    "runOnSave.commands": [
        {
            "match": ".*\\.php$",
            "command": "vendor/bin/pint --dirty --format agent",
            "runIn": "backend"
        },
        {
            "match": ".*\\.php$",
            "command": "php artisan ide-helper:generate && php artisan ide-helper:meta && php artisan ide-helper:models --nowrite",
            "runIn": "backend"
        }
    ],
    "css.lint.unknownAtRules": "ignore"
}